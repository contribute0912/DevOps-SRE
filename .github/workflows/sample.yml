
name: Dispatch Sanity

on:
  workflow_dispatch:   # manual button

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show context
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "Ref:   ${{ github.ref }}"
          echo "SHA:   ${{ github.sha }}"
